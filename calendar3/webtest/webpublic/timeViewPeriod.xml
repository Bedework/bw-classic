<?xml version="1.0"?>
<!DOCTYPE project SYSTEM "../WebTest.dtd"[
  <!ENTITY configPublic     SYSTEM "../ent/configPublic.xml">
]>
<!-- temp setup -->

<project name="timeViewPeriod" basedir="." default="main">
  <property name="webtest.home" location="/home/douglm/drop2/webtest/1.7" />

  <import file="${webtest.home}/lib/taskdef.xml"/>

  <!-- Import useful macros -->
  <import file="clientMacros.xml"/>

  <target name="main" depends="timeViewPeriod" />

  <target name="timeViewPeriod" >
    <webtest name="timeViewPeriod">
      &configPublic;

      <steps>
        <publicSetup />

        <repeat count="50">
          <verifyMainPage />

          <invoke description="go to previous view"
                  url="#{mainurlPrevViewperiod}" />
        </repeat>

        <repeat count="50">
          <verifyMainPage />

          <invoke description="go to next view"
                  url="#{mainurlNextViewperiod}" />
        </repeat>
      </steps>
    </webtest>
  </target>
</project>


