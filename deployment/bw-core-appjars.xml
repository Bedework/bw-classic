<?xml version="1.0"?>

<!-- ===================================================================
     Fetch jars for the core calendar application
     =================================================================== -->
<project name="extraJars" basedir="." default="">
  <macrodef name="getExtraJars">
    <sequential>
      <delete dir="${org.bedework.temp.extrajars.dir}" />
      <mkdir dir="${org.bedework.temp.extrajars.dir}" />

      <!-- =================== Bedework caldav jars ======================== -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-caldav" version="${org.bedework.bwcaldav.version}"
              projecthome="${bw.project.home}/../bwcaldav" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-cdvutil" version="${org.bedework.caldav.version}"
              projecthome="${bw.project.home}/../caldav" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-cdvserver" version="${org.bedework.caldav.version}"
              projecthome="${bw.project.home}/../caldav" />

      <!-- ===================== Bedework schema jars ====================== -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-synchwsschema" version="${org.bedework.bwxml.version}"
              projecthome="${project.home}/../bwxml" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-calws-soapschema" 
              version="${org.bedework.bwxml.version}" 
              projecthome="${project.home}/../bwxml" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-caldavschema" 
              version="${org.bedework.bwxml.version}" 
              projecthome="${project.home}/../bwxml" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-tzschema" version="${org.bedework.bwxml.version}"
              projecthome="${project.home}/../bwxml" />

      <!-- ================= Bedework dump/restore jars ==================== -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-dumpres" version="${org.bedework.dumprestore.version}"
              projecthome="${project.home}/../dumprestore" />

      <!-- ================= Bedework calendar core jars ================== -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-inoutsched" version="${org.bedework.calcore.version}"
              projecthome="${project.home}/../bwcalcore" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-calcorei" version="${org.bedework.calcore.version}" 
              projecthome="${project.home}/../bwcalcore" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-calcore" version="${org.bedework.calcore.version}" 
              projecthome="${project.home}/../bwcalcore" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-calsvc" version="${org.bedework.calcore.version}" 
              projecthome="${project.home}/../bwcalcore" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-chgnote" version="${org.bedework.calcore.version}" 
              projecthome="${project.home}/../bwcalcore" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-mail" version="${org.bedework.calcore.version}" 
              projecthome="${project.home}/../bwcalcore" />

      <!-- ================ Bedework calendar api jars ===================== -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-annotations" version="${org.bedework.annotations.version}" 
              projecthome="${project.home}/../bwannotations" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-calfacade" version="${org.bedework.calfacade.version}" 
              projecthome="${project.home}/../bwcalFacade" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-icalendar" version="${org.bedework.icalendar.version}" 
              projecthome="${project.home}/../bwical" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-interfaces" version="${org.bedework.interfaces.version}" 
              projecthome="${project.home}/../bwinterfaces" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-sysevents" version="${org.bedework.sysevents.version}" 
              projecthome="${project.home}/../bwsysevents" />

      <!-- =================== Bedework indexer jars ======================= -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-indexer" version="${org.bedework.indexer.version}"
              projecthome="${project.home}/../indexer" />

      <!-- ================== Bedework webapps jars ======================== -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-appcommon" version="${org.bedework.webapps.version}" 
              projecthome="${project.home}/../bwwebapps" />

      <!-- Only for portlet - maybe
        <getJar lib="${org.bedework.temp.extrajars.dir}"
                libcache="${org.bedework.libcache.dir}"
                name="bw-liferay" version="${org.bedework.webapps.version}" 
                projecthome="${project.home}/projects/webapps" />
                  
        <getJar lib="${org.bedework.temp.extrajars.dir}"
                libcache="${org.bedework.libcache.dir}"
                name="bw-uportal" version="${org.bedework.webapps.version}" 
                projecthome="${project.home}/projects/webapps" />
                -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-webcommon" version="${org.bedework.webapps.version}"
              projecthome="${project.home}/../bwwebapps" />

      <!-- =================== Bedework webdav jars ======================== -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-webdavserver" version="${org.bedework.webdav.version}"
              projecthome="${project.home}/../webdav" />

      <!-- ==================== Bedework other jars ======================== -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="rpiaccess" version="${org.bedework.access.version}" 
              projecthome="${project.home}/../access" />

      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="rpiutil" version="${org.bedework.rpiutil.version}" 
              projecthome="${project.home}/../rpiutil" />
              -->

      <getJar mvn="yes" lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="bw-util" version="${org.bedework.bw-util.version}" 
              projecthome="${org.bedework.git.project.home}/bw-util" />
      
      <!-- ======================= Ical4j jars ============================ -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="backport-util-concurrent" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="ical4j" version="1.0-BW-SNAPSHOT"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="ical4j-vcard" version="0.9.6-BW-SNAPSHOT"/>
      
      <!-- ======================= Commons jars ============================ -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-beanutils" version="1.7.0"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-codec" version="1.5"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-digester" version="1.7"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-fileupload" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-lang" version="2.6"/>
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-validator" version="1.3.0"/>

      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-logging" />
              -->

      <!-- ======================= httpclient jars ========================= -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="httpclient" version="4.2.1"/>
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="httpcore" version="4.2.2"/>

      <!-- ======================== json jars ============================== -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jackson-core" version="2.1.1" />
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jackson-annotations" version="2.1.1" />
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jackson-databind" version="2.1.1" />

      <!-- ========================= dkim jars ============================= -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="apache-jdkim-library" version="0.2.BEDEWORK-SNAPSHOT"/>
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="dnsjava" version="2.1.1"/>

      <!-- ======================= Hibernate jars ========================== -->
      
      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="antlr" version="2.7.6" />
              -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="asm" version="3.2" />
                      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="asm-attrs" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="cglib" version="2.1.3" />
      
      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="dom4j" version="1.6.1" />
              -->
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="ehcache" version="1.2.3" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="hibernate" version="3.6.9.Final" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="hibernate-validator" version="3.1.0.GA" />
      
      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="hibernate-annotations" version="3.4.0.GA" />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="javassist" version="3.4.GA" />
     
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jgroups" version="2.2.8" />
-->      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jta" version="1.1"  />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="commons-collections"/>
      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="slf4j-api" version="1.5.6"  />
      
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="slf4j-log4j12" version="1.5.6"  />
              -->

      <!-- ========================== Mail jars ============================ -->

      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="activation" version="1.4" />
              -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="dsn" version="1.4" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="imap" version="1.4" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="mailapi" version="1.4" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="pop3" version="1.4" />

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="smtp" version="1.4" />

      <!-- ========================== Misc jars ============================ -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="activemq-core" version="5.3.0" />

      <!--
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jakarta-oro" version="2.0.8"/>
              -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="geronimo-kernel" version="2.2.1"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="imgscalr-lib" version="4.2"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jstl" version="1.1.2"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="jstl-standard" version="1.1.2"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="lucene-core" version="2.0.0"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="lucene-misc" version="2.0.0"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="solr-solrj" version="3.6.0"/>
      
      <!-- Might be able to get rid of this if we use system xalan -->
      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="serializer" version="2.7.1"/>

      <!-- ========================== Web jars ============================= -->

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="gson" version="1.5"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="struts" version="1.2.9"/>

      <getJar lib="${org.bedework.temp.extrajars.dir}"
              libcache="${org.bedework.libcache.dir}"
              name="xalan" version="2.7.1"/>
    </sequential>
 </macrodef>
</project>
